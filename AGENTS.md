# gocel 开发项目规范

> **核心理念：** 优雅的软件源于纪律。不靠炫技，靠四个原则贯穿始终。

---

## 总则

本规范适用于本项目所有模块（gocel/core 与 gocel）的设计、开发与重构。任何代码提交、接口设计、模块拆分、依赖引入、Bug 修复，都须以以下原则为判据：

| # | 原则 | 英文 | 一句话 |
|---|------|------|--------|
| 1 | **单一职责** | Single Responsibility | 一个模块只做一件事，并且做好。 |
| 2 | **单一来源** | Single Source of Truth | 每份数据只有一个权威定义。 |
| 3 | **单向依赖** | Unidirectional Dependency | 依赖方向从高层流向低层，不循环。 |
| 4 | **核心组装** | Core Assembly | 核心稳定不可变，变化通过组装扩展。 |
| 5 | **排查修复** | Root-Cause Fixing | Bug 修在根因层，不散布补丁。 |
| 6 | **契约优先** | Contract over Defense | 源头拦截非法输入，不层层兜底。 |
| 7 | **测试先行** | Test-First | 无测试的改动叫"猜"，不叫"修"。 |
| 8 | **渐进重构** | Incremental Refactoring | 每次离开营地，让它比你发现时更干净。 |
| 9 | **最小惊奇** | Least Astonishment | 代码行为让读者一眼猜对。 |
| 10 | **分支开发** | Branch Development | 功能/修复在独立分支开发，测试通过后合入主干。 |

---


## 十大原则详述

### 一、单一职责

**做法：**
- 一个文件只关心一个概念层级，不混合数据定义与业务逻辑
- 函数超过 50 行，考虑拆成多个私有函数
- 抽出来的小函数放在调用它的公共函数下方

**验证：** 检查改动文件是否有大函数没拆分。用圈复杂度工具过一下改动过的函数，超过 15 标记审查。**超过 100 行的函数 CR 直接打回。**

### 二、单一来源

**做法：**
- 常量和魔术数字统一定义，引入使用，不硬编码
- 配置仅从配置文件读取，代码里不写死路径或 key
- 同一字段不在两个地方定义值，缓存只是加速副本不是权威来源

**验证：** 查看改动中是否有重复定义的常量值。检查是否在代码里硬编码了本应从配置读取的值。

### 三、单向依赖

**做法：**
- 依赖方向：对外 API → 编排层 → 核心抽象 → 实现层
- 接口定义在消费者侧（谁用谁定义），实现方只写 struct
- 领域包定义的接口例外：机制包自身定义的领域接口——只要消费方依赖领域包而非反向，即符合单向依赖
- 低层不能 import 高层包，工具函数不反向依赖业务

**验证：** `go vet` 检查循环依赖。对改动检查 import 路径方向是否正确，实现层是否有反向引用。

### 四、核心组装

**做法：**
- 对外 API 只导出用户必需的类型和函数，内部细节用非导出（小写首字母）
- 核心层只依赖语言标准库和最小抽象，不引入第三方框架
- 配置选项用 WithXxx 风格提供，不直接暴露结构体
- 扩展功能通过接口接入，核心不感知具体实现

**验证：** 检查对外 API 是否有不应暴露的符号。检查核心层是否引入了不该有的第三方依赖。

### 五、排查修复

**做法：**
1. 找到报错点的调用链上游，确定数据的真正来源
2. 在数据产生处修复，不在报错点加兜底
3. 删掉下游为这个 bug 撒的补丁
4. 修 bug 前先写能复现的测试，确认它 fail 再改代码

**验证：** 检查改动是否在多个文件新增了 nil / err 检查。如果是，说明没修在根因。修复后删掉的补丁代码应不少于新增的修复代码。

### 六、契约优先

**做法：**
- 参数校验只放在系统入口（公共构造函数、API 入口），返回 error
- 内部各层假设传入参数已合法，不做重复校验
- 校验规则违反时用断言快速失败，不静默吞错误或不返回兜底值

**验证：** 检查改动是否在非入口文件新增了参数校验或 nil 检查。检查是否有不该被吞掉的错误被静默处理了。

### 七、测试先行

**做法：**
- 修 bug：先写回归测试确认它 fail，再改代码，测试永久保留
- 新功能：先写测试确认因功能缺失而 fail，再实现
- 重构：确保改动前后测试全绿
- 测试函数名体现测试意图

**验证：** 检查改动是否附带对应测试。跑一遍改动的测试确保通过。**零测试覆盖的代码不允许合入主干。**

### 八、渐进重构

**做法：**
- 单次提交只做一件事：修 bug、加功能、重构三选一
- 修 bug 的提交只含修复代码 + 回归测试
- 重构的提交只改结构不改行为
- 每次改动后确保编译通过、测试通过

**验证：** 检查 commit 改动是否混杂了不同类型（如同一 commit 既有 bug fix 又有重构）。不允许混合提交合入主干。

### 九、最小惊奇

**做法：**
- 接口名用 +er 后缀（Runner, Storer）
- 错误变量用 ErrXxx 命名
- 包名用小写单数
- 函数名诚实：save 只保存，get 不修改状态
- 返回值类型稳定：列表没数据返回空列表不返回 nil

**验证：** 跑静态检查工具（`staticcheck`、`golint`）。`go vet` 通过是底线。

### 十、分支开发

**做法：**
- 功能开发或 Bug 修复在独立分支进行，不在 main 分支直接提交
- 分支命名规范：`feature/xxx`（新功能）、`fix/xxx`（修 Bug）、`refactor/xxx`（重构）
- 分支完成并通过全部测试后，发起 Pull Request 合入 main
- 合入前确保 `go build ./...`、`go vet ./...`、`go test ./...` 全部通过
- 合入后删除远程分支，保持仓库整洁

**验证：** 检查当前是否在 main 分支上直接提交。**禁止在 main 分支直接提交非文档性变更。**

---

## 附录：CR 一票否决红线

| 问题 | 说明 |
|------|------|
| 函数 > 100 行且未拆分 | 编辑器查看行数 |
| import 循环依赖 | `go vet` 报错 |
| 实现层定义了接口类型 | 检查是否有 interface 定义放在了实现侧而非消费者侧，判据是依赖方向而非包名 |
| 修 bug 在多个文件加 nil 检查 | 说明没修根因 |
| 零测试覆盖 | 改动缺少对应测试 |
| 混合提交 | 同一 commit 同时包含修 bug 和重构 |
| `go vet` 不通过 | 底线检查，不通过不能合入（examples/ 中的冗余换行警告除外） |
| 参数校验在内部重复 | 校验应集中在入口 |
| 未清理的调试输出 | `fmt.Println`/`fmt.Printf` 遗留在非测试文件 |
| **在 main 分支直接提交非文档变更** | 功能/修复必须在独立分支开发 |
